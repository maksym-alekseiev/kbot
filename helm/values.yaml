# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  registry: "ghcr.io"
  repository: "maksym-alekseiev"
  tag: "v1.0.2-bb93602"
  os: "linux"
  arch: "amd64"
secret:
  name: "kbot"
  tokenKey: "token"
  tokenName: "TELE_TOKEN"
securityContext:
  privileged: true
